// Moved to packages/hifz_core (facts are UUID-keyed on both tiers).
// This shim keeps every existing
// `package:quran_app/utils/id_generator.dart` import working unchanged.

export 'package:hifz_core/hifz_core.dart' show IdGenerator;
